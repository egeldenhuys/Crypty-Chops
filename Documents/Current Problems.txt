* Decrypting multiple files

* Should we store the key so the file can be re-encrypted quickly?
	- When the user chooses to encrypt a file that they have encrypted before
		the key will be loaded (from memory?, file?). Stored key needs to be secure.
* Company information

* "What will we be doing after we have released the software?"

* Add a file without encrypting it

* How will the File List be stored?
